stages:
  - lint

eslint:
  image: node:latest
  stage: lint
  script:
    - npm install -g eslint
    - eslint js

phpcs:
  image: texthtml/phpcs
  stage: lint
  script:
    - phpcs --standard=./phpcs.ruleset.xml **.php
